# Contributing

## Release

To create a new release, run the following command:

```bash
grunt release
```
 
It will automatically:

- Build the release
- Publish to npm
- Push a new git tag

You can also lint the code:

```bash
npm run lint
```